# Example env file for Hellofoto-web
# Copy this to `.env` and replace placeholders with real values.

REACT_APP_API_BASE_URL=http://localhost:4000/api
REACT_APP_UPLOAD_URL=http://localhost:4000/uploads
REACT_APP_STRIPE_KEY=pk_test_YOUR_PUBLIC_KEY
REACT_APP_GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_KEY
PORT=3000
PUBLIC_URL=/

# Notes:
# - Do NOT commit your `.env` file if it contains secrets (API private keys, database URLs, etc.).
# - Add `.env` to your .gitignore if not already ignored.
